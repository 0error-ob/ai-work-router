分析一个 AI 工作任务，生成可执行的工作流路由策略。

你的目标不是推荐"单一最佳模型"，而是判断：

- 哪些阶段值得使用强推理
- 哪些阶段可以使用廉价执行
- 哪些阶段应优先使用确定性验证器
- 什么时候需要升级、停止或人工审核
- 这个任务是否适合自动化

## 任务

[在此粘贴任务描述]

## 可选上下文

- 成本敏感度：
- 失败成本：
- 可用验证方式：
- 可用工具：
- 上下文大小：
- 是否涉及代码 / 文件 / 外部网页 / 数据：
- 当前工作流或已知痛点：

## 输出要求

按以下 9 节结构输出。

---

# AI Work Routing Card

## 1. 任务

用 1–2 句话复述任务。
不要扩写，不要替用户添加未给出的目标。

## 2. 任务类型

从以下类别中选择一个或多个：

- Answer / 问答
- Rewrite / 改写
- Extract / 抽取
- Classify / 分类
- Generate / 生成
- Reason / 推理
- Code / 代码
- Agentic / 多步代理任务
- Research / 调研
- Data analysis / 数据分析
- Decision / 决策
- Other / 其他

## 3. 任务结构画像

逐项判断，并给一句理由：

- Oracle 强度：强 / 中 / 弱 / 无
- 视野长度：单步 / 短多步 / 长程 / 开放式
- 歧义程度：低 / 中 / 高
- 上下文依赖：低 / 中 / 高
- 输出约束：自由文本 / 结构化输出 / 可执行代码 / patch / 决策建议 / 对外文本
- 失败成本：低 / 中 / 高
- 可回滚性：容易 / 中等 / 困难 / 未知
- 成本敏感度：低 / 中 / 高

## 4. 工作流拆解

按照以下阶段拆解任务。
如果某阶段不需要，明确写"不需要"并说明原因。

### Intake / 理解任务
- 目标：
- 推荐路由：
- 理由：

### Plan / 规划
- 目标：
- 推荐路由：
- reasoning 程度：
- 理由：

### Search / Context / 搜索与上下文收集
- 目标：
- 推荐路由：
- 理由：

### Execute / 执行
- 目标：
- 推荐路由：
- reasoning 程度：
- 理由：

### Verify / 验证
- 目标：
- 首选验证方式：
- 是否优先使用确定性验证器：
- 理由：

### Repair / 修复
- 触发条件：
- 推荐路由：
- 升级条件：

### Package / 交付
- 输出形式：
- 推荐路由：
- 理由：

## 5. 各阶段路由策略

用表格输出：

| 阶段 | 推荐模型类 / 工具类 | Reasoning 程度 | 具体动作 | 为什么 |
|---|---|---|---|---|

只能从这些模型类中选择，不要使用具体模型名：

- 强推理
- 廉价执行
- 可编程模型
- 长上下文
- 结构化输出稳定型
- 确定性验证器
- 人工审核

注意：

- 能用确定性验证器时，不要优先使用 LLM 判断。
- 能拆成 plan / execute / verify 的任务，不要把整个任务交给一个模型完成。
- 如果 prompt 本身不足以路由，明确说缺少什么信息。

## 6. 升级触发条件

列出具体、可执行的升级条件。

例如：

- 测试连续失败 2 次
- root cause 不清楚
- 修改范围扩大
- oracle 变弱
- 任务从低风险变成高风险
- 上下文缺失
- 输出无法验证
- 模型开始编造事实

不要写模糊条件，比如"情况复杂时升级"。

## 7. 停止 / 不可自动化条件

列出哪些情况下不应该继续自动化，必须人工介入。

特别关注：

- 弱 oracle + 高失败成本
- 不可回滚操作
- 安全 / 法律 / 财务 / 医疗风险
- 需求本身不清楚
- 缺少关键上下文
- 验证方式不存在
- 执行会影响真实用户或生产环境

## 8. Agent Instruction

最后生成一段可以直接复制给 coding agent / AI agent 的简短指令。

要求：

- 不超过 10 行
- 使用命令式语言
- 明确 plan / execute / verify / repair 的边界
- 明确什么时候升级或停止
- 不使用具体模型名

## 9. 说明

用 3–5 句话解释这个路由策略的核心逻辑。

重点回答：

- 为什么强推理应该花在这些阶段
- 为什么某些阶段可以用廉价执行
- 验证如何降低风险
- 什么时候不能继续自动化
